<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherNotesApp.Views"
    x:Class="WeatherNotesApp.Views.NotesPage"
    x:Name="ThisPage"
    Title="Notes">

    <VerticalStackLayout Padding="20" Spacing="10">
        <Entry
            Placeholder="Scrie o notiță..."
            Text="{Binding NoteText}" />

        <Button Text="Adaugă" Command="{Binding AddNoteCommand}" />
        <Button Text="Vezi Vremea" Clicked="OnWeatherButtonClicked"/>

        <CollectionView ItemsSource="{Binding Notes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Șterge"
                                   BackgroundColor="Red"
                                   Command="{Binding Source={RelativeSource AncestorType={x:Type local:NotesPage}}, Path=BindingContext.DeleteNoteCommand}"
                                   CommandParameter="{Binding .}" />
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <!-- Card vizual pentru fiecare notiță -->
                        <Frame Margin="5"
                       Padding="10"
                       BackgroundColor="#f8f8f8"
                       BorderColor="#ddd"
                       CornerRadius="12">
                            <Grid RowDefinitions="Auto, Auto">
                                <Label Grid.Row="0"
                               Text="{Binding Text}"
                               FontSize="18"
                               TextColor="Black" />
                                <Label Grid.Row="1"
                               Text="{Binding Date, StringFormat='{0:dd.MM.yyyy HH:mm}'}"
                               FontSize="12"
                               TextColor="Gray"
                               VerticalOptions="End"
                               Margin="0,5,0,0"/>
                            </Grid>
                        </Frame>

                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>